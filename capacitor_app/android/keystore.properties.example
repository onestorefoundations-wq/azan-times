# Copy to keystore.properties and fill in. Never commit the filled copy.
# The existing Play upload key lives at flutter_app/android/masjid-release.jks;
# reuse it only if this shell is meant to replace the Flutter app's listing.
storeFile=../../flutter_app/android/masjid-release.jks
storePassword=
keyAlias=masjid
keyPassword=
